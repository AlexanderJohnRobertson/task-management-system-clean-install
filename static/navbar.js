// This file contains the javascript functions for the navbar.
    function viewTasks() {
    location.replace("/viewtasks")
    } // Redirects to the viewtasks page
    function viewProjects() {
    location.replace("/viewprojects")
    } // Redirects to the viewprojects page
    function goHome() {
    location.replace("/userhome")
    } // Redirects to the userhome page

   function addTask() {
    location.replace("/addtask")
    } // Redirects to the addtask page

    function addProject() {
    location.replace("/addproject")
    } // Redirects to the addproject page

    function toIndex() {
    location.replace("/")
    } // Redirects to the index page

    function updateTask() {
    location.replace("/updatetask")
    } // Redirects to the updatetask page

    function updateProject() {
    location.replace("/updateproject")
    } // Redirects to the updateproject page

    function deleteTask() {
    location.replace("/deletetask")
    } // Redirects to the deletetask page

    function deleteProject() {
    location.replace("/deleteproject")
    }  // Redirects to the deleteproject page

    function changeUserName() {
    location.replace("/changeusername")
    } // Redirects to the changeusername page

    function changePassword() {
    location.replace("/changepassword")
    }

    function deleteAccount(){
    location.replace("/deleteaccount")
    } // Redirects to the deleteaccount page

    function viewAccountDetails() {
    location.replace("/accountdetails")
    } // Redirects to the accountdetails page

    function updateAccountDetails() {
    location.replace("/updateaccountdetails")
    } // Redirects to the updateaccountdetails page

    function viewUsers() {
    location.replace("/viewusers")
    } // Redirects to the viewusers page

    function blockUser() {
      location.replace("/blockuser")
    } // Redirects to the blockuser page

    function unblockUser() {
      location.replace("/unblockuser")
    } // Redirects to the unblockuser page

    function deleteUser() {
      location.replace("/deleteuser")
    } // Redirects to the deleteuser page

    function changeAccountType() {
      location.replace("/changeaccounttype")
    } // Redirects to the changeaccounttype page

    function changeUserPassword() {
      location.replace("/changeuserpassword")
    } // Redirects to the changeuserpassword page

    function reset() {
        location.replace("/reset")
    } // Redirects to the reset page